package spring_boot_jwt_system.model;

// Enum representing the different user roles
public enum RoleModel {
    // Constant for regular user role
    USER,
    // Constant for admin user role
    ADMIN
}
